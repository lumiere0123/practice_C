def inc_count(d, k):
    '''(dict of obj: int, immutable obj) -> None
    Update d by adding k with a value of 1 if k is not in d or 
    incrementing the value associated with k if k is in d.
    
    >>> d = {'coldplay': 1, 'the killers': 2}
    >>> inc_count(d, 'coldplay')
    >>> d
    {'coldplay': 2, 'the killers': 2}
    >>> inc_count(d, 'metallica')
    >>> d['metallica']
    1
    
    # TODO: other tests
    '''

    pass